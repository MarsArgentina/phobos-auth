export const {
	useCallback,
	useEffect,
	useState,
	useRef,
	useLayoutEffect,
	useMemo,
	useReducer,
	useContext,
	useDebugValue,
	useImperativeHandle,
} = wp.element;

export const { forwardRef, memo, lazy } = wp.element;
export const { Suspense, StrictMode } = wp.element;

export const { Component, Fragment, Children } = wp.element;

export const {
	createRef,
	createContext,
	createElement,
	cloneElement,
} = wp.element;

export const {
	isValidElement,
	concatChildren,
	switchChildrenNodeName,
} = wp.element;

export const {
	createPortal,
	findDOMNode,
	render,
	unmountComponentAtNode,
} = wp.element;

export default wp.element;
